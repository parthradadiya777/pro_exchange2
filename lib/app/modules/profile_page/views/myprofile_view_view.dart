import 'package:flutter/material.dart';

import 'package:get/get.dart';

class MyprofileViewView extends GetView {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MyprofileView'),
        centerTitle: true,
      ),
      body: Column(
        children: [

        ],
      )
    );
  }
}
